[Unit]
Description=repomap persistent repo-map daemon
After=network.target

[Service]
Type=simple
ExecStart=__BINARY_PATH__ daemon start --internal
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
